---
name: Setup Python
description: Install uv with the given Python version

inputs:
  python-version:
    required: true

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v7
      with:
        python-version: ${{ inputs.python-version }}
        enable-cache: true
